import React from 'react'

const PortfolioItem = ({name, description, tags, image, source_code_link}) => {
  return (
    <div>PortfolioItem</div>
  )
}

export default PortfolioItem

//name, description, tags[{name,color}], image, source_code_link